// let maxNum = parseInt(prompt('enter max number'));

// const targenNum = Math.floor(Math.random() * maxNum) + 1;
// const winningNum = [];

for (let i = 0, arr = []; i < 8; i ++){
    arr[arr.length] = (Math.floor(Math.random() * 50) + 1);
    let = winningNum = arr[i];
    console.log(winningNum);
}